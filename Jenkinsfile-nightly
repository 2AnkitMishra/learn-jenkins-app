pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                sh '''
                    docker version
                    docker ps
                    ls -a
                    docker build -t my-playwright .
                '''
            }
        }
    }
}